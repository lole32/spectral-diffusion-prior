[project]
name = "spectral-diffusion-prior"
version = "0.1.0"
description = ""
authors = [
    {name = "ELISAVET PARSELIA",email = "elisavet@omegacrop.com"}
]
readme = "README.md"
requires-python = "^3.9"
dependencies = [
    "scipy (>=1.10.0,<1.14.0)",
    "tqdm (>=4.67.1,<5.0.0)",
    "matplotlib (>=3.5.0,<3.8.0)",
    "scikit-image (>=0.19.0,<0.22.0)",
    "h5py (>=3.7.0,<3.10.0)",
    "rasterio (>=1.4.3,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
